<%= form_tag email_document_path(document), remote: true, id: "form" + document.id.to_s do %>
  <div class="row">
    <div class="large-8 columns">
      	
  	  <%= text_field_tag 'recipient_email', nil, placeholder: "email address"%> 
    </div> 
    <div class="large-4  columns">
      <%= submit_tag "send", class: "button small" %> 
  
    </div> 
  </div>

<% end %>
