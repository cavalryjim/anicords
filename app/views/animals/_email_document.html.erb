<%= form_tag email_document_path(document), remote: true, id: 'email_document_form' do %>
  <div class="row">
    <div class="large-8 columns">
      	
  	  <%= text_field_tag 'recipient_email', nil, placeholder: "email address"%> 
    </div> 
    <div class="large-4  columns">
      <%= submit_tag("send") %> 
  
    </div> 
  </div>

<% end %>
