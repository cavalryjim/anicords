<h1>Activities</h1>
<% @activities.each do |activity| %>
  <div class="activity">
  <%= link_to activity.owner.name_or_email, activity.owner if activity.owner %>
  <%= render_activity activity %>
  </div>
<% end %>
