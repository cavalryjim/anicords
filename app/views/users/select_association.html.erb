
<div class="full_page">
  <section class="gray_background ">
    <div class="row pad-btm">
      <div class="column large-12">
	    <h1 class="ptitle left">Associations</h1>
        <%= breadcrumb_nav(["Home"]) %>
	  </div>
    </div>
  </section>
 
 <section class="white_background">
	
  <div class="row pad-btm associations-main">
    <div class="large-12 columns">
      <ul class="associationList ">
       <% @user.user_associations.each do |association| %>
        <% next if cannot? :read, association.group %>
      	<li>
      	  <div class="medium-6 large-4 columns shop-product-small text-center pad-btm clearfix">
	        <div class="image">
	          <%= link_to image(association), association.group %>
	        </div>
	        <h2><%= link_to association.name, association.group %></h2>
	        <%= link_to button_small("go there"), association.group %>
	        
         </div>
        </li>
       <% end %>
      </ul>	
    </div>
  </div>
  <div class="row">
	<div class="large-12 columns">
	  <p>
	  	You are associated with <%= pluralize(current_user.associations.count, 'account') %>.  
	  	If needed, click <%= link_to button_small("here"), user_select_account_type_path %> to add another account.
	  </p>
	</div>
  </div>
 </section>
</div>
